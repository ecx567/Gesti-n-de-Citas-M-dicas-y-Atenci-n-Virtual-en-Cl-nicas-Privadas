# OpenSpec Configuration
# Auto-generated by sdd-init on 2026-05-30

context: >
  Mobile app for medical appointment scheduling and virtual care in private clinics.
  Built with Expo SDK 56 (React Native 0.85.3, React 19.2.3, TypeScript 6.0.3).
  Package manager: pnpm. Template: blank-typescript. Fresh project — no test runner,
  no CI/CD, no linter configured yet. Git connected to origin/main on GitHub.
  Jira managed under KAN project (KAN-14 UX/UI, KAN-15 Desarrollo MVP,
  KAN-16 Pruebas).

project: vitacitas-mobile

stack:
  runtime: react-native
  framework: expo
  language: typescript
  build: expo-cli

strict_tdd: false

testing:
  runner: null
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  tools:
    linter: null
    type_checker: tsc (via tsconfig strict)
    formatter: null

persistence:
  mode: hybrid
  backends:
    - engram
    - openspec

phases:
  explore:
    require_approval: true
  propose:
    require_approval: true
  spec:
    require_approval: true
  design:
    require_approval: true
  tasks:
    require_approval: true
  apply:
    require_approval: true
    enforce_strict_tdd: false
  verify:
    require_approval: true
  archive:
    require_approval: false

delivery:
  default_strategy: ask-on-risk
  review_budget: 400
  chain_strategy: stacked-to-main
